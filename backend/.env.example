PORT=8080
GRAPHIQL=true

# Queue Config
QUEUE_URL=amqp://localhost:5672
QUEUE_NAME=render

# GitHub Config
GH_CLIENT_ID=secret
GH_CLIENT_SECRET=secret
GH_CALLBACK_URL=secret
GH_WEBHOOK_SECRET=secret

# the base url where this app is deployed.
URL=secret

# Database Config
DB_USER=secret
DB_PASS=secret
DB_HOST=secret
DB_PORT=secret
DEV_DB_NAME=secret
PROD_DB_NAME=secret
DB_OPTIONS="{"retryWrites":"true","w":"majority"}"

SESSION_SECRET=secret
NODE_ENV=development

APOLLO_CORS=true
RETURN_MOCKS=true
# There needs to be a Github App private key file "app.pem" in the /backend directory